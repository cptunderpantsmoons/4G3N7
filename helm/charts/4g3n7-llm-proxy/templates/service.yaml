apiVersion: v1
kind: Service
metadata:
  name: {{ include "4g3n7-llm-proxy.fullname" . }}
  labels:
    {{- include "4g3n7-llm-proxy.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "4g3n7-llm-proxy.selectorLabels" . | nindent 4 }}